创建一个简单的IM页面。

技术栈：后端基于go,前端用react

功能列表:
1. [x] 整体功能和风格，类似于element或者telegram
2. [x] 支持基于room的群聊，以及dm单聊
3. [x] 可以@用户发送消息
4. [x] 可以创建Room
5. [x] 可以邀请用户加入Room
6. [x] 页面语言国际化，支持中英文切换

任务清单：
- [x] 交互简洁、界面清爽
- [x] 整体交互仍然保持简洁，但使用frontend-skill帮我美化一下页面
- [x] 把侧边栏头部改成两部分：上面只保留语言切换和 New Room 按钮，下面保留更紧凑的 3 个统计块；同时给统计标签强制单行，避免英文因为长度不同撑高。
- [x] 左侧边栏头部还是需要标注CSGClaw
- [x] 底层统一成一个会话模型，UI 默认不强调 Room/DM 分类，但保留一个轻量的 kind 或 is_private 属性处理权限和文案。这样代码和界面都会更简洁，又不容易引起语义混乱。
- [x] New Chat换成一个深色背景的醒目的+号
- [x] 语言切换改成 中 / EN 的分段开关，并保持现有中英文文案逻辑不变
- [x] 把头部拆成“品牌行 + 操作行”：第一行只放 CSGClaw 和右侧窄语言开关，第二行只放 + 按钮和统计块原有结构不动
