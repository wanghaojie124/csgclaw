.DS_Store

# Go build/test artifacts
.gocache/
bin/
bin-csghub/
dist/
*.test
*.out

# Web frontend local artifacts
node_modules/
web/app/playwright-report/
web/app/test-results/
web/app/.vite/
web/static-dist/*
!web/static-dist/
!web/static-dist/.gitkeep

# Local binaries
/csgclaw

# Local runtime data
manager-data/
docker/

# Generated embedded runtime workspaces
/internal/agent/embed/
# PicoClaw embed dist/ is populated by make/CI; do not commit (see make prepare-picoclaw-embed-dist).
# Note: go:embed ignores .gitignore, so these paths must not be listed here.

# Editor / tool directories
.cursor/
.idea/
.vscode/
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json

# Vendored native libraries / SDK dist fetched on demand
third_party/boxlite-go/libboxlite.a
third_party/boxlite-go/boxlite-c*/
